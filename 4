background(255);
size(500, 500);
colorMode(HSB, 100);
noStroke();
int y=480;
for (int x=20; x<455; x=x+25){
  while (y < 480) {
    fill(y/5, 100, 100);
    ellipse(x, y, 20, 20);
    y=y+25;
  }
  y=y-450;
}
